<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-gray-100 to-gray-300 min-h-screen flex items-center justify-center p-6">

  <div class="bg-white/80 backdrop-blur-md shadow-2xl rounded-2xl p-8 w-full max-w-lg">

    <!-- Title -->
    <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">✏️ Edit User</h1>

    <!-- Form -->
    <form action="/update" method="post" class="space-y-6">
      <input type="hidden" name="id" value="<%= user._id %>">

      <!-- Name -->
      <div>
        <label for="name" class="block text-gray-700 font-medium mb-2">Name</label>
        <input type="text" id="name" name="name" value="<%= user.name %>" required
          class="w-full px-4 py-2 border rounded-lg shadow-sm focus:ring-2 focus:ring-blue-500 focus:outline-none">
      </div>

      <!-- Email -->
      <div>
        <label for="email" class="block text-gray-700 font-medium mb-2">Email</label>
        <input type="email" id="email" name="email" value="<%= user.email %>" required
          class="w-full px-4 py-2 border rounded-lg shadow-sm focus:ring-2 focus:ring-blue-500 focus:outline-none">
      </div>

      <!-- Photo -->
      <div>
        <label for="photo" class="block text-gray-700 font-medium mb-2">Photo URL</label>
        <input type="text" id="photo" name="photo" value="<%= user.photo %>"
          class="w-full px-4 py-2 border rounded-lg shadow-sm focus:ring-2 focus:ring-blue-500 focus:outline-none">
        <div class="mt-4 flex justify-center">
          <img src="<%= user.photo %>" alt="User photo"
            class="w-24 h-24 object-cover rounded-full border-4 border-white shadow-md ring-4 ring-blue-500/30">
        </div>
      </div>

      <!-- Buttons -->
      <div class="flex justify-between items-center">
        <a href="/read"
          class="px-5 py-2 bg-gray-300 text-gray-800 font-semibold rounded-lg shadow hover:bg-gray-400 transition">
          ← Cancel
        </a>
        <button type="submit"
          class="px-6 py-2 bg-blue-600 text-white font-semibold rounded-lg shadow hover:bg-blue-700 transition">
          ✅ Update User
        </button>
      </div>
    </form>
  </div>

</body>

</html>
