<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Edit Post</title>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        neon: '#00ffff'
                    }
                }
            }
        }
    </script>
</head>

<body class="min-h-screen flex items-center justify-center 
             bg-gradient-to-br from-[#0f0f1a] via-[#141433] to-black text-white">

    <div class="w-[92%] max-w-xl rounded-2xl p-7 
                bg-white/5 backdrop-blur-xl 
                border border-white/10
                shadow-[0_0_40px_rgba(0,255,255,0.15)]
                animate-fade-in">

        <h1 class="text-center text-2xl font-semibold tracking-wide text-cyan-200 mb-6">
            ✏️ Edit Your Thought
        </h1>

        <form action="/updatePost/<%= post._id %>" method="post" class="space-y-4">

            <textarea 
                name="content"
                required
                class="w-full h-44 p-4 rounded-xl
                       bg-black/50 text-cyan-100
                       border border-cyan-400/30
                       focus:outline-none focus:ring-2 focus:ring-cyan-400
                       focus:border-cyan-400
                       resize-none
                       transition-all duration-300"
            ><%= post.content %></textarea>

            <button 
                type="submit"
                class="w-full py-3 rounded-xl font-semibold tracking-wide
                       bg-gradient-to-r from-cyan-400 to-blue-400
                       text-black
                       hover:shadow-[0_0_20px_rgba(0,255,255,0.7)]
                       hover:-translate-y-0.5
                       active:scale-95
                       transition-all duration-300"
            >
                Update Post
            </button>

        </form>

        <p class="mt-4 text-center text-sm text-white/60">
            Refine it. Sharpen it. Publish clarity.
        </p>
    </div>

    <!-- Small animation -->
    <style>
        @keyframes fade-in {
            from { opacity: 0; transform: translateY(16px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fade-in 0.6s ease-out;
        }
    </style>

</body>
</html>
