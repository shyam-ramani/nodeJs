<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Create User</title>

    <!-- Tailwind CDN -->
    <script src="https://cdn.tailwindcss.com"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        neon: '#00ffff'
                    }
                }
            }
        }
    </script>
</head>

<body class="min-h-screen flex items-center justify-center 
             bg-gradient-to-br from-[#0f0f1a] via-[#141433] to-black text-white">

    <div class="w-[92%] max-w-md rounded-2xl p-8
                bg-white/5 backdrop-blur-xl
                border border-white/10
                shadow-[0_0_45px_rgba(0,255,255,0.15)]
                animate-fade-in">

        <h1 class="text-center text-2xl font-semibold tracking-wide text-cyan-200 mb-6">
            🚀 Create Account
        </h1>

        <form action="/createUser" method="post" class="space-y-4">

            <input 
                type="text" 
                name="name" 
                placeholder="Your Name" 
                required
                class="w-full px-4 py-3 rounded-xl
                       bg-black/50 text-cyan-100
                       border border-cyan-400/30
                       focus:outline-none focus:ring-2 focus:ring-cyan-400
                       transition-all"
            />

            <input 
                type="email" 
                name="email" 
                placeholder="Email Address" 
                required
                class="w-full px-4 py-3 rounded-xl
                       bg-black/50 text-cyan-100
                       border border-cyan-400/30
                       focus:outline-none focus:ring-2 focus:ring-cyan-400
                       transition-all"
            />

            <input 
                type="password" 
                name="password" 
                placeholder="Password" 
                required
                class="w-full px-4 py-3 rounded-xl
                       bg-black/50 text-cyan-100
                       border border-cyan-400/30
                       focus:outline-none focus:ring-2 focus:ring-cyan-400
                       transition-all"
            />

            <button 
                type="submit"
                class="w-full py-3 rounded-xl font-semibold tracking-wide
                       bg-gradient-to-r from-cyan-400 to-blue-400
                       text-black
                       hover:shadow-[0_0_22px_rgba(0,255,255,0.7)]
                       hover:-translate-y-0.5
                       active:scale-95
                       transition-all duration-300"
            >
                Create User
            </button>

        </form>

        <!-- Links -->
        <div class="mt-6 flex justify-between text-sm text-white/70">
            <a href="/login" class="hover:text-cyan-300 transition">
                Already have an account?
            </a>
            <a href="/dashboard" class="hover:text-cyan-300 transition">
                Dashboard →
            </a>
        </div>
    </div>

    <!-- Entry animation -->
    <style>
        @keyframes fade-in {
            from { opacity: 0; transform: translateY(18px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .animate-fade-in {
            animation: fade-in 0.6s ease-out;
        }
    </style>

</body>
</html>
